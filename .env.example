NEXT_PUBLIC_ADMIN_API_URL=https://your-admin-backend-url.com
AGNES_API_KEY=your_agnes_api_key_here
CHAT_MODE=live # or 'mock' for development
# Robustness configuration
FETCH_TIMEOUT=30000 # Timeout in ms for Agnes API requests (default: 30000)
MAX_RETRIES=3 # Number of retry attempts for failed requests (default: 3)
RETRY_BASE_DELAY=1000 # Base delay in ms for retry backoff (default: 1000)
RATE_LIMIT_MAX=100 # Max requests per window (default: 100)
RATE_LIMIT_WINDOW=60000 # Window in ms for rate limiting (default: 60000 = 1 minute)
